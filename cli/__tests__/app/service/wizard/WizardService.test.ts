import { associateStableCoinService } from '../../../../src/index';

describe('Testing Wizard class', () => {
  it('Should render', async () => {
    associateStableCoinService;

    expect(true).not.toBe(null);
  });
});
